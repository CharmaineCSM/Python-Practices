''' Question 1: Sum of Integers Up To n (integersums.py)

Write a function, add_it_up(), that takes a single integer as input and returns the sum of the integers from zero to the input parameter.

The function should return 0 if a non-integer is passed in.'''

Solution:

def add_it_up(num):
    try:
        result = sum(range(0,num+1))
    except TypeError:
        result = 0
    return result

print(add_it_up(4))
    
